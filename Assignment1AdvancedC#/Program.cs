using Assignment1AdvancedC_.classes;
using System;

namespace Assignment1AdvancedC_
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // 1. The Bubble Sort algorithm has a time complexity of O(n^2) in its worst and average cases,
            //    which makes it inefficient for large datasets. How we can optimize the Bubble Sort algorithm
            //    And implement the code of this optimized bubble sort algorithm 
            #region Q1(Bubble Sort)
            //int[] intArray = { 64, 34, 25, 12, 22, 11, 90 };
            //Console.WriteLine("Unsorted int array: " + string.Join(", ", intArray));
            //Helper<int>.BubbleSort(intArray);
            //Console.WriteLine("Sorted int array: " + string.Join(", ", intArray)); 
            #endregion


            #region Q2(Range)
            //// Integer range
            //Range<int> intRange = new Range<int>(5, 15);
            //Console.WriteLine("Is 10 in range? " + intRange.IsInRange(10)); // True
            //Console.WriteLine("Is 20 in range? " + intRange.IsInRange(20)); // False
            //Console.WriteLine("Length of int range: " + intRange.RangeLength()); // 10

            //// Double range
            //Range<double> doubleRange = new Range<double>(1.5, 5.5);
            //Console.WriteLine("Is 3.0 in range? " + doubleRange.IsInRange(3.0)); // True
            //Console.WriteLine("Is 6.0 in range? " + doubleRange.IsInRange(6.0)); // False
            //Console.WriteLine("Length of double range: " + doubleRange.RangeLength()); // 4.0 
            #endregion
        }
    }
}
